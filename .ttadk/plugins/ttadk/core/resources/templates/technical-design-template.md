# Technical Design Document Template

<!--
============================================================
TEMPLATE INSTRUCTIONS (DO NOT COPY TO OUTPUT DOCUMENT)
============================================================
The following guidelines are for reference only.
DO NOT include this section in the generated document.
============================================================
-->

<!--
**Document Guidelines**:
- This is a **design document**, not implementation code
- Focus on **what to change**, not **how to write code**
- **Conditional Sections**: Only include sections relevant to your changes

**Mermaid Syntax Requirements (Target version: 9.1.x)**:
- All diagrams use Mermaid (`graph TD`, `sequenceDiagram`, `erDiagram`, `stateDiagram-v2`)
- Node text must use double quotes: `A["Text"]` instead of `A[Text]`
- Arrow types: Only use `-->` (solid), `-.->` (dashed), `==>` (thick)
- Arrow labels must be simple: `A -->|Label| B` (letters, numbers, spaces, basic punctuation only)
- ER diagram decimal type: Use `decimal` without parameters
- Subgraph syntax: `subgraph Title` instead of `subgraph Name[Text]`
-->

<!--
============================================================
ACTUAL DOCUMENT CONTENT STARTS HERE
============================================================
-->

> Generated by [**TTADK**](https://bytedance.larkoffice.com/wiki/Gw0ewxEbHi1K0NkVd2YcNwvVnTg) (TikTok AI-Driven Development Kit)


## 1. Business Background

**Objective**: [One sentence describing the core goal]

**Measurable Targets**:
- [Target 1]
- [Target 2]

---

## 2. Requirement Analysis

### 2.1 Current Status

[Brief description of the current system state]

```mermaid
graph LR
    A["Current Step 1"] --> B["Current Step 2"]
    B --> C["Current Step 3"]
```

### 2.2 Business Goals

1. **[Goal 1]**: [Description]
2. **[Goal 2]**: [Description]

### 2.3 Feature Module Division

| Feature Module | Feature Points | Change Type | Dependencies |
|:---------------|:---------------|:------------|:-------------|
| [Module Name] | [Feature description] | New | [Service/None] |
| | [Another feature] | Modified | [Service] |
| [Module Name] | [Feature description] | New | [Service] |

### 2.4 Use Case Diagram (Optional)

**Roles**:
- **User**: [Actions: submit, view, query, etc.]
- **Admin**: [Actions: configure, manage, etc.]

```mermaid
graph LR
    User["User"] --> A["Action 1"]
    User --> B["Action 2"]
    Admin["Admin"] --> C["Manage"]
```

---

## 3. System Design

### 3.1 Network Topology

```
[Describe network architecture if applicable]

Client --> Gateway --> Service Layer --> Storage Layer
                           |
                           └── External Dependencies
```

### 3.2 Overall Architecture Diagram

> Highlight changed modules with red style: `style ModuleName fill:#ff6b6b`

```mermaid
graph TD
    subgraph Client
        A["Frontend"]
    end
    subgraph Gateway
        B["API Gateway"]
    end
    subgraph Services
        C["Business Service"]
        D["Support Service"]
    end
    subgraph Storage
        E["MySQL"]
        F["Redis"]
        G["ES"]
    end
    A --> B
    B --> C
    C --> D
    C --> E
    C --> F
    C --> G
    style C fill:#ff6b6b
```

### 3.3 Data Synchronization (If applicable)

[Describe data sync strategy between systems]

| Source | Target | Sync Method | Frequency |
|:-------|:-------|:------------|:----------|
| [System A] | [System B] | [MQ/API/CDC] | [Real-time/Batch] |

### 3.4 Domain Model / ER Diagram (If applicable)

> Include: Core entities, relationships, changed tables/fields
> Table columns should be concise to display in single line on Lark

<!--
============================================================
ER DIAGRAM RELATIONSHIP SYMBOLS (Reference Only)
============================================================
Symbol meanings:
- ||--||  : One-to-One (exactly one on both sides, strong composition)
- ||--o|  : One-to-Zero-or-One (one required, other optional)
- ||--o{  : One-to-Many (one required, zero or more on many side)
- ||--|{  : One-to-Many (one required, at least one on many side)
- }o--o{  : Many-to-Many (both sides optional)

Symbol breakdown:
- ||  = Exactly one (required)
- o|  = Zero or one (optional single)
- |{  = One or more (required multiple)
- o{  = Zero or more (optional multiple)

Common mistakes:
- ❌ ||--o| for composition → Use ||--|| (component always exists with parent)
- ❌ }|--|| for config → Use ||--|| (each entity has exactly one config)
============================================================
-->

**Database Tables Example**:

```mermaid
erDiagram
    USER ||--o{ ORDER : "places"
    ORDER ||--|{ ORDER_ITEM : "contains"
    ORDER_ITEM }o--|| PRODUCT : "references"

    USER {
        bigint id "PK"
        string name "Username"
        int status "Status 0-inactive 1-active"
    }

    ORDER {
        bigint id "PK"
        bigint user_id "FK to USER"
        decimal total_amount "Order total"
        int status "Order status"
    }

    ORDER_ITEM {
        bigint id "PK"
        bigint order_id "FK to ORDER"
        bigint product_id "FK to PRODUCT"
        int quantity "Item quantity"
    }

    PRODUCT {
        bigint id "PK"
        string name "Product name"
        decimal price "Unit price"
    }
```

**Class/Object Relationships Example** (for C++/Java/non-DB scenarios):

```mermaid
erDiagram
    Processor ||--|| VideoNode : "owns"
    Processor ||--|| AudioNode : "owns"
    Processor ||--|| TrackManager : "manages"
    Processor ||--o| EventListener : "notifies"
    TrackManager ||--o{ TrackHandle : "pool"
    TrackHandle ||--|| TrackConfig : "uses"

    Processor {
        bool initialized "Init status"
        bool enabled "Global switch"
        ptr config "Configuration"
        ptr context "Runtime context"
    }

    VideoNode {
        bool enabled "Node enabled"
        uint output_texture "Output texture ID"
        list composer_nodes "Composer list"
    }

    AudioNode {
        bool enabled "Node enabled"
        int sample_rate "Audio sample rate"
        int channels "Channel count"
    }

    TrackManager {
        int max_tracks "Max track count"
        list track_pool "Track pool"
    }

    TrackHandle {
        int track_index "Track index"
        bool enabled "Track enabled"
        ptr native_handle "SDK handle"
    }

    TrackConfig {
        list sticker_paths "Sticker paths"
        list sticker_ids "Sticker IDs"
        string config_json "Config JSON"
    }
```

**Relationship Writing Rules** (CRITICAL):

1. **Always write from LEFT to RIGHT**: `LeftEntity ||--o{ RightEntity`
2. **Left side symbols**: `||` (exactly one) or `}o` (zero or more) or `}|` (one or more)
3. **Right side symbols**: `||` (exactly one) or `o|` (zero or one) or `o{` (zero or more) or `|{` (one or more)
4. **Read as**: "LeftEntity [has] RightEntity"

**Relationship Examples**:

| Scenario | Correct Syntax | How to Read |
|:---------|:---------------|:------------|
| One-to-One | `USER \|\|--\|\| PROFILE` | User has exactly one Profile |
| One-to-ZeroOrOne | `USER \|\|--o\| AVATAR` | User has zero or one Avatar |
| One-to-Many | `USER \|\|--o{ ORDER` | User has zero or more Orders |
| One-to-AtLeastOne | `ORDER \|\|--\|{ ORDER_ITEM` | Order has one or more OrderItems |
| Many-to-Many | `USER }o--o{ ROLE` | Users and Roles (many-to-many) |

### 3.5 Schema Definitions

> Only include subsections relevant to your changes

#### 3.5.1 Database Tables (If DB changes involved)

##### [Table Name]

| Property | Value |
|:---------|:------|
| **Table Name** | table_name |
| **Description** | [Table purpose] |
| **Data Estimate** | [Volume estimate] |
| **Idempotency** | [Yes/No] |
| **Change Impact** | [Migration needed?] |

**DDL**:
```sql
CREATE TABLE `table_name` (
  `id` bigint unsigned NOT NULL AUTO_INCREMENT COMMENT 'PK',
  `name` varchar(255) NOT NULL COMMENT 'Name',
  `status` int NOT NULL DEFAULT '0' COMMENT 'Status',
  `created_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `updated_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_status` (`status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

#### 3.5.2 Elasticsearch Index (If ES changes involved)

##### [Index Name]

| Property | Value |
|:---------|:------|
| **Index Name** | index_name |
| **Description** | [Purpose] |
| **Shards** | [Number] |
| **Replicas** | [Number] |

**Mapping**:
```json
{
  "mappings": {
    "properties": {
      "id": { "type": "keyword" },
      "name": { "type": "text" },
      "created_at": { "type": "date" }
    }
  }
}
```

#### 3.5.3 Cache Structure (If cache involved)

##### [Cache Name]

| Property | Value |
|:---------|:------|
| **Key Pattern** | `prefix:{id}` |
| **Type** | String/Hash/List/Set |
| **TTL** | [Duration] |
| **Purpose** | [Cache purpose] |

**Value Example**:
```json
{
  "id": "123",
  "data": "value"
}
```

#### 3.5.4 MQ Messages (If MQ involved)

##### [Message Name]

| Property | Value |
|:---------|:------|
| **Topic** | topic.name |
| **Purpose** | [Message purpose] |
| **Format** | JSON |
| **Consumer** | [Service name] |
| **Idempotency** | [How handled] |
| **QPS** | [Expected volume] |

**Message Structure**:
```go
type MessageName struct {
    ID        string `json:"id"`
    Type      string `json:"type"`
    Payload   Data   `json:"payload"`
    Timestamp int64  `json:"timestamp"`
}
```

#### 3.5.5 Configuration (If TCC/Config involved)

| Config Key | Type | Default | Example | Purpose |
|:-----------|:-----|:--------|:--------|:--------|
| `config.key` | JSON | `{}` | `{"enabled": true}` | [Purpose] |

---

## 4. Core Changes

> For each interface or change point:
> - **Flowchart** → **Sequence Diagram** → **Field Change Table**
> - Combine all flows (main, branch, exception, async) into ONE comprehensive flowchart
> - **Language**: Use ONLY ONE language throughout (zh or en based on preferred_language setting)

### 4.1 [Interface/Change Name]

**Flowchart**

```mermaid
graph TD
    Start["Start"] --> A["Validate Input"]
    A --> B{"Check Permission"}
    B -->|Fail| ERR1["Return 403"]
    B -->|Pass| C["Process Request"]
    C --> D{"Check Condition"}
    D -->|Case A| E1["Process Type A"]
    D -->|Case B| E2["Process Type B"]
    E1 --> F["Save to DB"]
    E2 --> F
    F --> G{"Success?"}
    G -->|No| H["Log Error"]
    H --> I{"Retryable?"}
    I -->|Yes| F
    I -->|No| J["Rollback & Alert"]
    J --> ERR2["Return Error"]
    G -->|Yes| K["Update Cache"]
    K --> L["Send MQ Message"]
    L --> End["Return Success"]
    ERR1 --> End2["End"]
    ERR2 --> End2
    style C fill:#ff6b6b
    style F fill:#ff6b6b
```

**Sequence Diagram**

```mermaid
sequenceDiagram
    participant Client
    participant Gateway
    participant Service
    participant DB as Database
    participant Cache as Redis
    participant MQ

    Client->>Gateway: Request
    Gateway->>Service: Forward
    Service->>Service: Validate & Check Permission
    alt Permission Denied
        Service-->>Client: 403 Error
    else Permission OK
        Service->>DB: Save Data
        alt DB Error
            Service->>DB: Rollback
            Service-->>Client: Error Response
        else DB Success
            Service->>Cache: Update Cache
            Service->>MQ: Send Message
            Service-->>Client: Success Response
        end
    end
```

**Field Changes**

| Field | Operation | Logic |
|:------|:----------|:------|
| field_a | Read | Query from table_x by id |
| field_b | Write | Update status to 1 |
| field_c | Read/Write | Read from cache, write to DB if miss |

### 4.2 [Another Interface/Change Name]

**Flowchart**

```mermaid
graph TD
    Start["Start"] --> A["Receive Request"]
    A --> B["Check Cache"]
    B --> C{"Cache Hit?"}
    C -->|Yes| D["Return Cached Data"]
    C -->|No| E["Query DB"]
    E --> F["Update Cache"]
    F --> G["Transform Response"]
    D --> H["End"]
    G --> H
```

**Sequence Diagram**

```mermaid
sequenceDiagram
    participant Client
    participant Service
    participant Cache
    participant DB

    Client->>Service: Request
    Service->>Cache: Check cache
    alt Cache Hit
        Cache-->>Service: Return data
    else Cache Miss
        Service->>DB: Query data
        DB-->>Service: Return data
        Service->>Cache: Update cache
    end
    Service-->>Client: Response
```

**Field Changes**

| Field | Operation | Logic |
|:------|:----------|:------|
| user_id | Read | Get from request |
| data | Write | Save to DB |

### 4.3 [Async Consumer Name] (If async flow exists)

**Flowchart**

```mermaid
graph TD
    Start["Receive Message"] --> A["Parse Message"]
    A --> B{"Valid?"}
    B -->|No| C["Send to DLQ"]
    B -->|Yes| D["Query Related Data"]
    D --> E["Process Business Logic"]
    E --> F{"Success?"}
    F -->|No| G["Retry or DLQ"]
    F -->|Yes| H["Update Status"]
    H --> I["Ack Message"]
    C --> I
    G --> I
    I --> End["End"]
```

**Sequence Diagram**

```mermaid
sequenceDiagram
    participant MQ
    participant Consumer
    participant DB
    participant ExternalAPI

    MQ->>Consumer: Push Message
    Consumer->>Consumer: Parse & Validate
    alt Invalid Message
        Consumer->>MQ: Send to DLQ
    else Valid Message
        Consumer->>DB: Query Related Data
        Consumer->>ExternalAPI: Call External Service
        ExternalAPI-->>Consumer: Response
        Consumer->>DB: Update Status
    end
    Consumer->>MQ: Ack
```

**Field Changes**

| Field | Operation | Logic |
|:------|:----------|:------|
| message_id | Read | From MQ message |
| status | Write | Update after processing |

### 4.3 State Diagram (If applicable)

> Include: Initial state, intermediate states, terminal states, exception paths

```mermaid
stateDiagram-v2
    [*] --> Created
    Created --> Processing
    Processing --> Completed
    Processing --> Failed
    Completed --> [*]
    Failed --> [*]
```

### 4.4 Interface Definitions (If applicable)

> Format: PSM + Method, request/response body (no tables), separator after each interface

#### service.psm.name / MethodName

**Request**:
```thrift
struct RequestName {
    1: required string field1    // Field description
    2: optional int64 field2     // Field description
    255: base.Base Base
}
```

**Response**:
```thrift
struct ResponseName {
    1: int32 code               // Response code
    2: string message           // Response message
    3: DataType data            // Response data
    255: base.BaseResp BaseResp
}
```

---

#### service.psm.name / AnotherMethod

**Request**:
```thrift
struct AnotherRequest {
    1: required int64 id
    255: base.Base Base
}
```

**Response**:
```thrift
struct AnotherResponse {
    1: int32 code
    2: string message
    255: base.BaseResp BaseResp
}
```

---

## 5. Checklist

> DDL changes and resource application tasks only

- [ ] [DDL task: Create/alter table xxx]
- [ ] [DDL task: Add index on table xxx]
- [ ] [Resource: Apply for Redis cluster]
- [ ] [Resource: Apply for MQ topic]
- [ ] [Resource: Apply for ES index]
